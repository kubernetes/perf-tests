# Total number of volumes will be one per node.
PODS_PER_NODE: 1
VOLUMES_PER_POD: 1
NODES_PER_NAMESPACE: 1

# Disable pod creation.
START_PODS: false

# Wait for creation instead of volume binding.
WAIT_FOR_PVS_CREATED: true
WAIT_FOR_PVS_BOUND: false
